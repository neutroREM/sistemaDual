@model IEnumerable<sistemaDual.Models.Universidad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UniversidadID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreU)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <tr>
                <td>
                @Html.DisplayFor(modelItem =>  item.UniversidadID)
            </td>
                <td>
                    @Html.DisplayFor(modelItem =>  item.NombreU)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UniversidadID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UniversidadID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UniversidadID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
